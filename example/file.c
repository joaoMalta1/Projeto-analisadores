#include <stdio.h>

void alerta(const char* n_dev, const char* msg);

void alerta_var(const char* n_dev, const char* msg, int var);

void ligar(const char* n_dev);

void desligar(const char* n_dev);

int main() {
    return 0;
}
